#include <iostream>
#include <list>
#include <set>
#include <vector>
#include <stdio.h>
#include <algorithm>
#include <queue>

using namespace std;
#define rep(i, n) for (int i = 1; i < n + 1; i++)

int main()
{
    int n, m;
    long long k;
    cin >> n >> m >> k;
    long long sum = 0;

    rep(i, n)
    {
        rep(i, m)
        {
        }
    }
}